# Big Data Project

All our written code is in the notebooks folder.

The raw file was loaded manually into data/raw. With the data cleaning notebook and the transformation notebook the raw file was respectively loaded, transformed, and saved into data/processed. Models were saved through joblib and torch saving functionality into the models folder. Important figures can be found in the reports/figures folder.
